import React from 'react'
import Carousel from 'react-bootstrap/Carousel'



const Slider = ({start}) => {
  return (
    <div>
        <Carousel>
  {start.map}
  </Carousel>
    </div>
  )
}

export default Slider